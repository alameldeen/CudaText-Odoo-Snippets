name=Odoo OnChange Method
id=oonchangemethod
lex=Python
text=
@api.multi
@api.onchange("${1:Fields}")
def _onchange_${2:Field}(self):
    for rec in self:
        rec.${2:Field} = ${4:Value}
       ${10}